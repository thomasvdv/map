# Cloudflare R2 Configuration
# ===========================
#
# Required for uploading VFR tiles, satellite tiles, and maps to Cloudflare R2 storage.
# Copy this file to .env and fill in your actual values.
#
# To use:
#   1. Copy this file: cp .env.example .env
#   2. Fill in your R2 credentials below
#   3. Load variables: source .env
#   4. Upload content: python -m olc_downloader.cli upload-to-r2 --all
#

# Your Cloudflare account ID
# Find this in your Cloudflare dashboard URL: dash.cloudflare.com/<ACCOUNT_ID>/
R2_ACCOUNT_ID=your-account-id-here

# R2 API Token - Access Key ID
# Create this in Cloudflare Dashboard > R2 > Manage R2 API Tokens
R2_ACCESS_KEY_ID=your-access-key-id-here

# R2 API Token - Secret Access Key
# This is shown only once when you create the token
R2_SECRET_ACCESS_KEY=your-secret-access-key-here

# Optional: Custom R2 bucket name (default: "map")
# R2_BUCKET=map

# Optional: Custom public domain for R2 bucket
# Set this up in Cloudflare Dashboard > R2 > [Your Bucket] > Settings > Public access
# R2_PUBLIC_DOMAIN=https://your-custom-domain.com
